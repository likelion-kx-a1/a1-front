/** 이미지 생성 페이지 (독립 라우트 — 프로젝트 없이 생성) */

import ImageGenerationView from "@/features/image-generation/components/ImageGenerationView";

export default function ImageGenerationPage() {
  return <ImageGenerationView />;
}
