/** 비디오 생성 페이지 (독립 라우트 — 프로젝트 없이 생성) */

import VideoGenerationView from "@/features/video-generation/components/VideoGenerationView";

export default function VideoGenerationPage() {
  return <VideoGenerationView />;
}
